@model Task.WebUI.Models.ArtAbout
@{
    ViewBag.Title = "ArtAbout";
}
<style>
    .card {
        min-width: 100%;
    }
</style>

<div class="container-fluid">
    <div class="card">
        <div class="card-header text-center">
            <h2>@Model.Article.Name</h2>
        </div>
        <div class="card-body">

            <p>@Model.Article.Text</p>

        </div>
        <div class="card-header">
            @foreach(Task.Domain.Entities.Tag tag in Model.Tags)
            {
                @Html.ActionLink(tag.Name, "Index",new { tag_id=tag.Id},new { @class = "btn btn-dark" })
            }
        </div>
    </div>


</div>


